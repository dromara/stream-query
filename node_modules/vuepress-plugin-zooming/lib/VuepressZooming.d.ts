import Zooming from 'zooming';
export declare class VuepressZooming {
    instance: Zooming;
    constructor();
    update(selector?: string): void;
    updateDelay(selector?: string, delay?: number): void;
}
